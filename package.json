{
  "name": "predictype",
  "version": "0.11.0",
  "description": "PredicType is a library of pre-built and tested predicates for TypeScript, covering various data types and operations.",
  "homepage": "https://maduhaime.github.io/predictype/",
  "keywords": [
    "predicate",
    "typescript",
    "type-safe",
    "validation",
    "comparison",
    "membership",
    "range",
    "state"
  ],
  "license": "MIT",
  "author": "Marc-Antoine Duhaime",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maduhaime/predictype.git"
  },
  "type": "module",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/esm/index.d.ts",
        "default": "./dist/esm/index.js"
      },
      "require": {
        "types": "./dist/cjs/index.d.ts",
        "default": "./dist/cjs/index.js"
      }
    }
  },
  "scripts": {
    "build:cjs": "tsc -p tsconfig.cjs.json",
    "build:esm": "tsc -p tsconfig.esm.json",
    "build": "rimraf dist && npm run build:cjs && npm run build:esm",
    "code:check": "npm run lint && npm run format:check",
    "code:pack": "npm run build && npm pack",
    "code:prepublish": "npm run code:check && npm run test && npm run build",
    "code:publish": "npm publish --access public",
    "docs": "rimraf docs && typedoc",
    "docs:deploy": "npm run docs && gh-pages -d docs -f",
    "lint": "eslint 'src/**/*.ts'",
    "lint:fix": "eslint --fix 'src/**/*.ts'",
    "format:check": "prettier --check 'src/**/*.{ts,json,md}'",
    "format:write": "prettier --write 'src/**/*.{ts,json,md}'",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.62.0",
    "@typescript-eslint/parser": "^8.62.0",
    "@vitest/coverage-v8": "^4.1.9",
    "eslint": "^10.5.0",
    "eslint-plugin-prettier": "^5.5.6",
    "gh-pages": "^6.3.0",
    "prettier": "^3.8.4",
    "rimraf": "^6.1.3",
    "typedoc": "^0.28.19",
    "typedoc-plugin-markdown": "^4.12.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
